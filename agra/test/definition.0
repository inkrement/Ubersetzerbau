func x(a b c)
  /* Schreibender var zugriff auf vorh. var */
  let x = 0; y = 1; in
    x = 3;
    y = 2;
  end;
end;
func x(a b c)
  /* Schreibender var zugriff auf vorh. var */
  let abcd = 0; in
    a = 3;/*test*/
    (3+3);
    notExistingFunc(a,b,c);
  end;
/*test*/
end;
